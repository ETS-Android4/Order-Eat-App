<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.store">
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Store">
        <activity android:name=".change_number"></activity>
        <activity android:name=".number" />
        <activity android:name=".change_email" />
        <activity android:name=".E_mail" />
        <activity android:name=".change_password" />
        <activity android:name=".checkPassword" />
        <activity android:name=".change_name" />
        <activity android:name=".cart" />
        <activity android:name=".massege" />
        <activity android:name=".settings" />
        <activity android:name=".profile" />
        <activity android:name=".show_details" />
        <activity android:name=".realStart"
            tools:ignore="ExtraText">
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>
        <activity
            android:name=".Sign_in_form"
            android:label="Sign in" />
        <activity
            android:name=".Start"
            android:label="login" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>